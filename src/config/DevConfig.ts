import { AppConfig } from './dataModels';

const DEV_CONFIG: AppConfig = {
	// Take the api url form BioT Console technical page
	API_URL: `https://api.env.customer.biot-med.com`
};

export default DEV_CONFIG;
